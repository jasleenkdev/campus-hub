MONGO_URI=
JWT_SECRET=
PORT=8000

# Local development only. Set to "true" to raise the rate limits (global
# 100 -> 2000, auth 10 -> 200 per 15 min) so automated checks are not locked
# out. Ignored when NODE_ENV=production. Leave unset everywhere else.
RATE_LIMIT_RELAXED=

# Only the public API base URL belongs here.
# Never put MONGO_URI or JWT_SECRET in a frontend env file — anything
# prefixed NEXT_PUBLIC_ is inlined into the browser bundle.
NEXT_PUBLIC_API_URL=http://localhost:8000
